FORMULAS

Synopsis:
	A list of formulas for things such as pokeballs and chances

POKEBALLS:

ball = checkball

	if (ball = master)
		pokemon = caught
	else
		if (ball = poke)
			ballrate = genrand(0 255)
			#generate number between 0 and 255
		elif (ball = great)
			generate number between 0 and 200
		else
			generate number between 0 and 150 (ultra and safari)
		fi
